核心思路：

migrations/ 目录存放带编号的迁移文件，打进每次更新 ZIP
db_migrations 表记录已执行过的迁移，防止重复
每条迁移都用 INFORMATION_SCHEMA 先判断再操作，绝不破坏现有数据
更新流程 apply 之后自动触发 db_migrate 步骤
Created 3 filesCreated 3 files现在更新两个核心文件，把 db_migrate 步骤接入更新流程：Ran a command, edited a fileRan a command, edited a file现在更新前端，把 db_migrate 步骤插入执行队列，并调整进度百分比：Add db_migrate step to the update queue and adjust percentagesAdd db_migrate step to the update queue and adjust percentages还要让日志能显示迁移详情（迁移成功了哪些条目）：Edited a file, ran a command, read a fileEdited a file, ran a command, read a file5 个文件，部署方式如下：
文件放置位置admin_update_api.phpadmin/admin_update_api.php（替换原文件）admin_update.phpadmin/admin_update.php（替换原文件）DbMigrator.phpinclude/DbMigrator.php（新增）001_initial_schema.phpmigrations/001_initial_schema.php（新建目录）_TEMPLATE.phpmigrations/_TEMPLATE.php（参考模板，可不部署）

整体机制说明：
迁移如何保证安全：

建表全用 CREATE TABLE IF NOT EXISTS，已有表绝不重建
加列先查 INFORMATION_SCHEMA.COLUMNS，列已存在直接跳过
每条迁移在事务里执行，失败自动回滚，不留半成品
db_migrations 表记录已执行迁移名，重复运行系统会跳过

更新流程变化（新增第6步）：
备份 → 下载 → 校验 → 解压 → 覆盖文件 → 数据库迁移 ← 新增 → 清理
以后每次发版加新表/字段的写法：
复制 _TEMPLATE.php，命名为 002_xxx.php，写 up() 函数，打进 ZIP 包的 migrations/ 目录即可。用户无论从哪个版本升级，系统都会自动找出未执行的迁移按顺序补全。